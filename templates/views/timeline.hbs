{{>header}}

<body>
  {{>navbar}}

  <!-- Main Page -->
  <div class="container-fluid p-5 text-bg-light">

    <div class="container-xl mb-4">
      <h4>Browse the history of nursing in Saskatchewan and Alberta by timeline.</h4>

      <a class="btn btn-primary" href="#" role="button">Pre 1900's</a>

      <button onclick="test()" class="btn btn-primary" name="searchByPre1900" id="noble" style="font-size: 1.8rem">
        Pre 1900's
      </button>

    </div>
    {{!-- Each Pre1900 articles is shown --}}
    {{!-- <div class="data-container"></div> --}}
    <div id="pre1900article"></div>

    <button id="toggleButton">Toggle Element</button>
    <div id="elementToToggle">This is the element to toggle</div>

    {{>footer}}
  </div>

  {{>script}}
  {{>dataFetch}}